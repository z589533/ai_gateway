岗位：后端工程师（偏全栈）　
耗时：4 小时以内（含 AI 工具）　
技术栈：Go（框架自选）
说明：可使用任意 AI 编程工具；架构判断、需求假设、trade-off 需自行完成并写进文档
背景
实现一个 AI Gateway MVP：统一管理租户 API Key，代理 AI 模型请求（下游可 mock），并记录用量。
重点考察：快速学习能力、架构设计、工程组织与交付。
最小要求
1. 租户与 Key 管理 — 创建 tenant / 多个 Key；支持 scope、启用/禁用、过期（scope 建模自定，文档说明）
2. AI 代理 — 见下方「接口兼容」；处理 401 / 403 / 502 / 504
3. 用量追踪 — 按 tenant + Key 记录 model、token、时间戳；支持查询
4. OpenAPI — 完整 OpenAPI 3.x spec，与实现一致
交付物
- GitHub 仓库，docker compose up 一键启动
- README：架构说明、运行步骤（含 curl）、设计决策、已知限制
选做（非必须）
- 简易 Dashboard — 极简前端页面，用于 Key 管理与用量查看即可
提交
1. 推送 GitHub 仓库
2. 将仓库链接发给招聘负责人
提交前自测
- docker compose up 可运行
- 可创建 tenant / Key
- 调用方可用 OpenAI 风格请求体调用 /v1/chat/completions
- 至少 1 个 model 走通代理（mock 即可）；无效 / 过期 / 越权 Key 会被拒绝
- 用量可查询，OpenAPI 与主要接口一致